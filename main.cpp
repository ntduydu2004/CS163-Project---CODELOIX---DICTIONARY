#include <iostream>


using namespace std;

#include "HashTable.h"


int main() {

    HashTable What(31, 400); //Init(P, N) (P < N, Better if P and N are prime numbers)

    return 0;
}
